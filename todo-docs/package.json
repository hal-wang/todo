{
  "name": "todo-docs",
  "scripts": {
    "dev": "cd ../todo-api && npm run doc && cd ../todo-docs && vuepress dev docs",
    "build": "cd ../todo-api && npm run doc && cd ../todo-docs && vuepress build docs && bash build.sh"
  }
}
